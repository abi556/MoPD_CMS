interface:
  display_name: "MoPD CMS"
  short_description: "Repo-specific patterns and workflows for MoPD_CMS"
  default_prompt: "Use the MoPD_CMS repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true