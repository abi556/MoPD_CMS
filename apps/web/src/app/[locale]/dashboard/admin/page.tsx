import { EmptyState } from "@/components/ui/empty-state";

export default function DashboardAdminPage() {
  return (
    <EmptyState
      title="Administration"
      description="Administration modules are scaffolded and will be expanded in admin phases."
    />
  );
}
